# fabric-swarm-manager
An implement of setup scripts for new fabric-swarm participant

# Install
 0. initialize submodule
 
    `./install.sh gitSync`
 
 1. install 
 
    `./install.sh`

# Bootstrap
 0. `./manager.sh down|up`
  
